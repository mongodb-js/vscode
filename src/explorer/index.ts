import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
  // TODO: lucas: Make task for setting up basic explorer tree
  // data provider and view.
  //
  // state.treeDataProvider = new ...
  // state.treeView = vscode.window.createTreeView('MongoDBExplorer', {
  //   treeDataProvider: ext.treeDataProvider
  // });
}

export function deactivate() { }
