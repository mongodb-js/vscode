// These are prompts for certain participant handlers where we can't write a definitive accuracy test.
// There isn't a discrete, codable true/false method for evaluating these.
// Instead, this is useful for manually verifying the results of how the model handles each.
