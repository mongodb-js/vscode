import * as React from 'react';

class ConnectionStatus extends React.Component {
  render(): React.ReactNode {
    return (
      <div>
        Coming soon - connection status.
      </div>
    );
  }
}

export default ConnectionStatus;
